"""
recommender.py — 부산 공공기관 직무 추천 엔진 (v2)
=========================================================
busan_data v2의 (직무 × 전형) 통계를 받아, 학생 프로필에 대해 '데이터상
유리한 직무'를 설명가능하게 스코어링하고 준비 로드맵을 만든다.

v1 대비 핵심 수정
  1) [도메인] 블라인드 채용 반영. 일반행정(사무·행정)은 학력·전공 무관이므로
     전공 적합도를 모든 계열에 1.0으로 둔다(인문계 0.8 페널티 삭제). 단,
     전공 무관이라는 사실을 그대로 노출해 '적합도'를 경쟁·규모 축으로 설명한다.
  2) [도메인] 자격 로드맵에서 '기사 + 산업기사' 중복 권장을 제거한다.
     동일 분야는 상위 자격(기사) 하나만 권장한다.
  3) [설계] 가중치를 외부 주입(weights)으로 받는다. 앱에서 슬라이더로 조절 →
     '하드코딩 점수'가 아니라 '사용자 조절형 시뮬레이터'가 된다.
  4) [정직성] 사회배려는 가짜 직무가 아니라, 추천된 각 직무의 '사회배려 전형
     경쟁률 갭'으로 보여준다(예: 전기직 일반 48:1 → 사회배려 3:1).
  5) [근거] 어학·자격 가이드는 임의값이 아니라 공고 임용조건(eligibility)에서
     추출한 실제 최저 기준을 인용한다.
"""
from __future__ import annotations
import math, statistics
from collections import defaultdict
import busan_data as bd

# ── 전공계열 → 지원가능 직무 적합도 ──
# 기술직은 자격·전공이 실제로 요구되므로 적합도 차등이 정당하다.
# 사무·행정은 블라인드(전공 무관)이므로 모든 계열에 1.0을 부여한다.
MAJOR_FIT = {
    "전기·전자":      {"전기": 1.0, "신호·통신": 0.85, "전산": 0.6, "사무·행정": 1.0},
    "기계":          {"기계": 1.0, "사무·행정": 1.0},
    "토목·건축·도시":  {"토목·건축": 1.0, "사무·행정": 1.0},
    "컴퓨터·정보":     {"전산": 1.0, "신호·통신": 0.7, "사무·행정": 1.0},
    "상경·행정·법":    {"사무·행정": 1.0, "전산": 0.4},
    "인문·사회·기타":  {"사무·행정": 1.0, "운전·운송": 0.7, "공무직·기능": 0.7},
}

# 블라인드(전공 무관) 직무 — 적합도 설명을 다르게 표기한다.
BLIND_JOBS = {"사무·행정", "운전·운송", "공무직·기능"}

# 직무별 권장 자격 — '기능사 이상' 같은 막연한 표기를 버리고, 공기업 합격자들이
# 실제로 준비하는 자격을 구체적으로 명시한다(출처: 아래 TRACK_GUIDE의 근거).
# 동일 분야 중복(기사+산업기사)은 상위 1개만, 단 '쌍기사' 관행은 자격현실에 명시.
TRACK_PREP = {
    "전기":      ["전기기사"],
    "기계":      ["일반기계기사 또는 공조냉동기계기사"],
    "토목·건축":  ["토목기사 또는 건축기사(지원 직렬에 맞춰 택1)"],
    "신호·통신":  ["정보통신기사"],
    "전산":      ["정보처리기사"],
    "사무·행정":  ["한국사능력검정 1급", "컴퓨터활용능력 1급"],
    "운전·운송":  ["제2종 전기차량 운전면허"],
    "공무직·기능": ["전기기능사", "소방안전관리자 2급"],
}

# 직무별 '필기시험 구성' — 기관·직렬마다 다르다. 운영직(사무·행정)은 전공이
# 아니라 일반상식을 본다는 점이 핵심(흔한 오해). 기술직은 NCS+전공.
# 출처: 부산교통공사 운영직 필기 = NCS 50% + 일반상식 50%(부교공/통합채용 공고·
# 합격자 후기). 기술직 = NCS + 전공시험.
TRACK_EXAM = {
    "사무·행정":  "NCS 직업기초능력 + 일반상식 (운영직은 전공시험이 아니라 상식을 봅니다)",
    "운전·운송":  "NCS 직업기초능력 + 직무 관련 평가",
    "공무직·기능": "서류·실기 중심 (기관별로 필기 없는 경우도 있음 — 공고 확인)",
    "전기":      "NCS 직업기초능력 + 전공시험(전기)",
    "기계":      "NCS 직업기초능력 + 전공시험(기계)",
    "토목·건축":  "NCS 직업기초능력 + 전공시험(토목/건축)",
    "신호·통신":  "NCS 직업기초능력 + 전공시험(신호·통신)",
    "전산":      "NCS 직업기초능력 + 전공시험(전산)",
}

# 직무별 '합격자 준비 현실' — 데이터(경쟁률·합격선)와 별개로, 외부 취업정보에서
# 확인된 합격자들의 실제 준비 수준. 데이터가 아니라 '참고 가이드'이므로 UI에서
# 데이터 수치와 시각적으로 분리하고 출처를 함께 표기한다(정직성).
# 근거: 공기업 채용 일반(나무위키 공공기관/채용), 시설관리 직무(나무위키 시설관리),
#       부교공 운영직 합격 후기 — '핸드오프 1원칙'에 따라 데이터로 위장하지 않는다.
TRACK_GUIDE = {
    "사무·행정": dict(
        한줄="운영직은 전공시험이 아니라 NCS+일반상식 시험입니다. 자격증 점수는 합격자 대부분이 "
            "비슷해서 변별이 작고, 필기 점수가 사실상 당락을 가릅니다.",
        합격자스펙=["한국사능력검정 1급(거의 전원 보유)", "컴퓨터활용능력 1급",
                  "토익 800대 중반(합격 후기 예: 815·865) — 어학은 충족용이지 변별 요소는 아님",
                  "경영/경제 전공자 다수 + 테셋·매경테스트 등 경제상식 자격(선택)"],
        현실="채용 구조가 '서류 적부(통과/탈락만) → 필기에서 약 100배수를 1.5배수로 압축 → "
            "면접 1.5배수'라, 사실상 필기 점수 하나로 합격이 결정됩니다. 면접은 변별력이 작습니다. "
            "그래서 합격자들은 자격증보다 'NCS 주 2회 모의고사 스터디 + 일반상식 기본서 회독 + "
            "최신 시사 구독'으로 필기 실전 점수를 끌어올리는 데 시간을 씁니다. 컴활·한국사만 따고 "
            "필기 대비가 얕으면 100:1을 못 넘습니다.",
        출처="부산교통공사 운영직 합격 후기 기반(외부 취업정보·공고 아님)",
    ),
    "전기": dict(
        한줄="전기직은 기사 1개가 '최소'고, 경쟁이 치열해 전기기사+전기공사기사 '쌍기사'가 "
            "사실상 서류 통과의 기본선입니다(중복 가산 인정).",
        합격자스펙=["전기 쌍기사(전기기사 + 전기공사기사) — 둘 다 중복 가산되는 기관 많음",
                  "토익 850 이상 또는 오픽 IH 이상",
                  "정보처리기사·컴활 1급·KBS한국어 중 택1",
                  "한국사능력검정 1급",
                  "전공시험 고득점(전공 90~100 + NCS 60~70 ≈ 평균 80+ 목표)"],
        현실="전기 쌍기사는 이미 '기본 스펙' 취급이라, 여기에 어학·한국사·IT 자격을 더해야 서류 "
            "안정권이 됩니다. 1~2점 차이로 서류 당락이 갈립니다. 다행히 공기업 NCS·전공 과목이 "
            "기사 시험 과목과 겹쳐 동시 대비가 가능합니다. 단, 가산 점수는 기관마다 천차만별이라 "
            "(예: 한전 쌍기사 최대 20점, 코레일 8점) 반드시 지원기관 공고로 확인하세요.",
        출처="전기직 공기업 합격 후기 기반(외부 취업정보·공고 아님)",
    ),
    "기계": dict(
        한줄="기계직은 일반기계기사를 1순위로 따고, 시설·설비 쪽은 공조냉동기계기사를 더한 "
            "쌍기사가 흔합니다(기계설비법 선임 가능).",
        합격자스펙=["일반기계기사(최우선·공기업이면 사실상 필수)",
                  "+ 공조냉동기계기사(시설관리·설비 직렬 쌍기사)",
                  "토익 800+ · 한국사 1급 · 컴활/정처기 중 택1",
                  "전공시험 고득점(NCS+전공, 평균 80+ 목표)"],
        현실="일반기계기사가 기본선이고, 시설관리·설비 직렬을 노리면 공조냉동기계기사를 더해 "
            "쌍기사로 가는 게 정석입니다. 전공시험에서 점수를 벌어야 합니다.",
        출처="기계직 공기업 합격 후기 기반(외부 취업정보·공고 아님)",
    ),
    "토목·건축": dict(
        한줄="지원 직렬(토목 vs 건축)에 맞는 기사를 먼저, 여력이 되면 상위·인접 기사를 추가합니다.",
        합격자스펙=["토목기사 또는 건축기사(직렬 맞춤)", "인접 분야 기사 추가 시 가산·변별 ↑",
                  "전공시험 대비"],
        현실="직렬에 정확히 맞는 기사부터 확보하는 게 우선입니다.",
        출처="공기업 합격 후기 기반(외부 취업정보·공고 아님)",
    ),
    "신호·통신": dict(
        한줄="정보통신기사를 축으로, 비전공이면 NCS+전공 인강으로 전공 기초를 메웁니다.",
        합격자스펙=["정보통신기사", "(비전공) 정통기·무선설비 등 전공 포커스 학습"],
        현실="표본이 얕은 직렬이라 공고별 모집인원 변동이 큽니다. 전공시험 대비가 관건.",
        출처="공기업 합격 후기 기반(외부 취업정보·공고 아님)",
    ),
    "전산": dict(
        한줄="정보처리기사가 기본축이고, SQLD·ADsP 등 데이터 자격이 가점·변별로 작용합니다.",
        합격자스펙=["정보처리기사", "SQLD/ADsP 등 데이터 자격(변별)", "전공시험 대비"],
        현실="전산 직렬은 표본이 적어 공고마다 편차가 큽니다. 전공 비중이 높습니다.",
        출처="공기업 합격 후기 기반(외부 취업정보·공고 아님)",
    ),
    "공무직·기능": dict(
        한줄="시설관리·유지보수는 전기기능사 + 소방안전관리자 2급이 기본 세트입니다.",
        합격자스펙=["전기기능사(기본)", "소방안전관리자 2급", "공조냉동기계기능사·가스기능사·위험물기능사 "
                  "중 직무 관련 1개", "여력 되면 전기산업기사 이상(운영사·정규직 경쟁력)"],
        현실="시설관리 운영사·공공기관 정규직을 노리면 기능사 하나로는 부족하고, 전기산업기사 "
            "이상으로 올라가는 게 정석입니다. 단순 기능사 1개+NCS로 합격을 기대하긴 어렵습니다.",
        출처="공기업 합격 후기 기반(외부 취업정보·공고 아님)",
    ),
    "운전·운송": dict(
        한줄="해당 차량 운전 면허가 응시 전제이고, 그 위에 NCS·직무평가를 준비합니다.",
        합격자스펙=["제2종 전기차량 운전면허 등 직무 면허", "NCS 대비"],
        현실="면허가 응시자격 자체인 경우가 많아 면허 취득이 1순위입니다.",
        출처="공기업 합격 후기 기반(외부 취업정보·공고 아님)",
    ),
}

DEFAULT_WEIGHTS = dict(fit=0.45, comp=0.35, size=0.20)
# [정직성] v2.1에서 'trend(추세)' 축을 제거함. 기존 _trend()는 모든 직무에 항상
# '유지'(0.6)를 반환해, 슬라이더가 있어도 점수에 아무 변별을 주지 못하는 '가짜
# 조절'이었다. 직무 단위 연도추세는 표본이 얕아 신뢰할 수 없으므로(핸드오프 8),
# 추세는 점수에서 빼고 '신규채용 추세 차트'(앱 탭2)에서 정보로만 보여준다.


def _competition_score(comp):
    """경쟁률이 낮을수록 유리 → 0~1. 범위를 3:1~200:1로 넓혀 변별력 확보."""
    if not comp:
        return 0.5
    lo, hi = 3.0, 200.0
    s = 1 - (math.log10(comp) - math.log10(lo)) / (math.log10(hi) - math.log10(lo))
    return round(max(0.0, min(1.0, s)), 3)


def _sample_score(n):
    """'규모'가 아니라 '데이터 표본량(추정 신뢰도)'. 레코드 수가 많을수록 그 직무의
       경쟁률 추정이 더 안정적이라는 의미일 뿐, 실제 채용 인원 크기가 아니다."""
    return min(1.0, (n or 0) / 8.0)


def build_job_view():
    """직무별 표시용 통계 묶음(앱·추천 공용). build_dataset 1회로 해결."""
    return bd.build_dataset()


def recommend(student: dict, top_k=3, weights=None, ds=None):
    """student = {학년, 전공계열, 보유자격[], 어학{토익}, 사회배려:bool}
       weights = {fit, comp, size} (합 1.0 권장, 앱 슬라이더 / 추세축은 v2.1에서 제거)
       반환: 직무별 적합도 + 설명 구성 + 로드맵
    """
    w = dict(DEFAULT_WEIGHTS)
    if weights:
        w.update(weights)
    ds = ds or bd.build_dataset()
    job_stats = ds["job_stats"]
    elig = ds["eligibility"]

    fit_map = MAJOR_FIT.get(student.get("전공계열", ""), {})
    grade = student.get("학년", 2)
    years_left = max(0.5, 4 - grade + 0.5)
    # [세분화] 장애/보훈/취업지원은 법적으로 다른 집단이라 하나의 Boolean으로
    # 뭉치지 않는다. 사용자가 실제 해당하는 전형만 추려서, 그 전형의 경쟁률만
    # '정보'로 보여준다(점수에는 반영하지 않는다 — 아래 주석 참고).
    내전형 = []
    가산 = student.get("가산", {})
    if 가산.get("장애인"):
        내전형.append("장애")
    if 가산.get("취업지원대상자"):
        내전형.append("취업지원")
    # 보훈은 입력 위젯이 따로 없어 취업지원대상자에 포함되는 경우가 많음(국가보훈부
    # 증명). 데이터에 보훈 전형이 있으면 취업지원대상자에게 함께 안내한다.

    results = []
    for job, fit in fit_map.items():
        s = job_stats.get(job)
        if not s or not s["일반_가중경쟁률"]:
            continue

        gen_comp = s["일반_가중경쟁률"]
        comp_s = _competition_score(gen_comp)
        # [중요 수정] 과거엔 사회배려 경쟁률을 점수(comp_s)에 max()로 섞었다. 그러면
        # 장애인 등록자에게 '장애+보훈+취업지원 혼합 경쟁률'로 점수를 줘서, 본인이
        # 실제 지원 가능한 전형이 아닌 값으로 추천이 흔들렸다. 이제 점수는 항상
        # '일반전형' 기준으로만 매기고, 사회배려는 정보로만 분리해 보여준다.

        size = _sample_score(s["일반_n"])           # '규모' 아님 → 데이터 표본량(신뢰도)
        score = (w["fit"] * fit + w["comp"] * comp_s + w["size"] * size)

        # 사용자 본인 해당 전형의 세부 경쟁률만 추림(없으면 빈 dict)
        내전형_데이터 = {}
        for t in 내전형:
            d_t = s.get("사회배려_세부", {}).get(t)
            if d_t and d_t.get("경쟁률"):
                내전형_데이터[t] = d_t
        social_info = bool(내전형_데이터)

        results.append(dict(
            직무=job, 적합도=round(score * 100),
            블라인드=job in BLIND_JOBS,
            구성=dict(전공적합=round(fit, 2), 경쟁여유=round(comp_s, 2),
                      표본량=round(size, 2)),
            사회배려적용=social_info,
            데이터=dict(일반경쟁률=gen_comp, 사회배려경쟁률=s["사회배려_가중경쟁률"],
                       사회배려세부=내전형_데이터,
                       합격선평균=s["합격선평균"], 합격선표준편차=s["합격선표준편차"],
                       합격선기관=s.get("합격선기관", []),
                       합격선_기관별=s.get("합격선_기관별", {}),
                       신뢰=s.get("신뢰", {}),
                       분류=s["분류"]),
            로드맵=_roadmap(job, s, elig.get(job, {}), student,
                          years_left, social_info, 내전형_데이터),
        ))
    results.sort(key=lambda r: -r["적합도"])
    return results[:top_k]


def _roadmap(job, s, elig, student, years_left, social_applied, 내전형_데이터=None):
    내전형_데이터 = 내전형_데이터 or {}

    # ── 필기 목표: 기관별로 분리(통계 착시 제거) ──
    # 과거엔 교통+도시를 섞은 단일 평균에 버퍼를 더해 '하나의 목표점수'를 만들었다.
    # 두 기관은 시험 과목이 달라 그 평균은 무의미하므로, 기관별 합격선을 각각 보여주고
    # 기관별 목표(합격선+버퍼)를 따로 제시한다.
    cut_by_inst = s.get("합격선_기관별", {}) or {}
    필기기관별 = []
    for inst_, dd in cut_by_inst.items():
        avg = dd.get("평균")
        std = dd.get("표준편차")
        if avg:
            buf = round(std) if std else 8
            필기기관별.append(dict(기관=inst_, 합격선평균=round(avg),
                                  목표=round(avg) + buf, 버퍼=buf, 표본=dd.get("n")))
    필기기관별.sort(key=lambda d: -d["목표"])

    if 필기기관별:
        # 카드/배너용 한 줄: 가장 높은 기관 기준(보수적). 기관명을 함께 노출.
        top_t = 필기기관별[0]
        target = (f"필기 {top_t['목표']}점 목표 ({top_t['기관'].replace('부산','')} 합격자 평균 "
                  f"{top_t['합격선평균']}점 + 안전 {top_t['버퍼']}점)")
    else:
        target = "이 직무는 합격선 공시가 없어요 — 공고·기출로 목표 점수를 잡으세요"

    필기구성 = TRACK_EXAM.get(job, "NCS 직업기초능력 + 직무 평가 (공고 확인)")

    have = set(student.get("보유자격", []))
    need = [c for c in TRACK_PREP.get(job, []) if not any(h in c for h in have)]

    # ── 합격자 준비 가이드(외부 취업정보 — 데이터 아님, 출처 표기) ──
    guide = TRACK_GUIDE.get(job)

    # 어학: 임의값이 아니라 공고 임용조건의 실제 최저 토익을 인용
    toeic = student.get("어학", {}).get("토익")
    lang = None
    min_toeic = elig.get("토익최저")
    if min_toeic:
        msg = f"공고상 토익 최저 {min_toeic}점 사례 확인됨"
        if toeic:
            gap = "충족" if toeic >= min_toeic else f"{min_toeic-toeic}점 부족"
            msg += f" (현재 {toeic} — {gap})"
        lang = msg

    blind_note = None
    if job in BLIND_JOBS:
        blind_note = "이 직무는 전공·학력 제한이 없는 블라인드 채용이라, 전공보다 " \
                     "NCS 필기·경쟁률 관리가 핵심입니다."

    # 신뢰도 경고: 경쟁률 표본이 얕거나 단일공고에 쏠렸으면 정직하게 알린다.
    conf = s.get("신뢰", {})
    conf_note = None
    if conf:
        if conf.get("등급") == "낮음":
            conf_note = (f"⚠️ 이 경쟁률은 {conf.get('한줄','표본 적음')}이라 참고용입니다 — "
                         "당해 공고로 반드시 재확인하세요.")
        elif conf.get("최대공고집중도") and conf["최대공고집중도"] >= 0.5:
            conf_note = (f"이 경쟁률은 단일 공고가 {int(conf['최대공고집중도']*100)}%를 "
                         "차지해, '5년 평균'보다 특정 연도 대형공고에 가깝습니다.")

    social_note = None
    if 내전형_데이터:
        gen_r = s["일반_가중경쟁률"]
        bits = []
        labels = {"장애": "장애인", "취업지원": "취업지원대상자", "보훈": "보훈"}
        for t, d_t in 내전형_데이터.items():
            bits.append(f"{labels.get(t, t)} 전형 {d_t['경쟁률']}:1(표본 {d_t['n']}건)")
        social_note = (f"내가 해당하는 전형 기준: 일반 {gen_r}:1 → "
                       + " / ".join(bits)
                       + ". 단, 전형별 응시자격·증빙은 공고에서 꼭 확인하세요.")

    return dict(
        필기목표=target,
        필기구성=필기구성,
        필기기관별=필기기관별,        # [신규] 기관별 합격선·목표 분리
        취득권장자격=need or ["권장 자격은 이미 보유 — 필기 점수에 집중"],
        합격자가이드=guide,            # [신규] 외부 취업정보 기반 합격자 준비 현실
        어학=lang,
        준비기간=f"약 {years_left:.1f}년",
        블라인드안내=blind_note,
        신뢰경고=conf_note,
        사회배려안내=social_note,
    )


def find_detours(student, recs, ds=None, min_gap=2.0):
    """[독창성 엔진] '전략적 우회 경로'를 정직하게 계산한다.

    핵심 원칙 — 합격확률·예측은 만들지 않는다. 두 트랙의 '실제 경쟁률 비율'(나눗셈)과
    '준비 자격의 차이'만 제시한다. "합격확률 N% 상승"(거짓) ✗ → "경쟁률이 약 N배 낮은
    트랙"(사실) ○.

    두 종류의 우회를 찾는다:
      (1) 직렬 우회 : 같은 전공 적합권 안에서, 1순위보다 경쟁이 의미있게(min_gap배+)
                     낮고 신뢰등급이 양호/보통인 트랙. (1순위가 이미 저경쟁이면 안 뜸)
      (2) 전형 우회 : 사용자가 실제 해당하는 가산 전형(장애/취업지원)이 있으면,
                     점수에 반영하지 않은 '일반 vs 해당전형' 경쟁 격차를 정보로 노출.
    """
    if not recs:
        return dict(직렬우회=[], 전형우회=[])
    ds = ds or bd.build_dataset()
    job_stats = ds["job_stats"]
    primary = recs[0]
    p_comp = primary["데이터"]["일반경쟁률"]
    fit_map = MAJOR_FIT.get(student.get("전공계열", ""), {})
    have = set(student.get("보유자격", []))

    # (1) 직렬 우회
    track_detours = []
    if p_comp:
        for job, fit in fit_map.items():
            if job == primary["직무"]:
                continue
            s = job_stats.get(job)
            if not s or not s["일반_가중경쟁률"]:
                continue
            conf = s.get("신뢰", {})
            if conf.get("등급") == "낮음":      # 신뢰 낮은 셀로는 우회 권하지 않는다(정직성)
                continue
            alt = s["일반_가중경쟁률"]
            if alt <= 0 or p_comp / alt < min_gap:
                continue                        # 의미있는 격차(기본 2배)만
            need = [c for c in TRACK_PREP.get(job, []) if not any(h in c for h in have)]
            track_detours.append(dict(
                대안직무=job, 대안경쟁률=alt, 현재직무=primary["직무"], 현재경쟁률=p_comp,
                경쟁배수=round(p_comp / alt, 1),
                신뢰등급=conf.get("등급"), 신뢰한줄=conf.get("한줄"),
                준비자격=need or ["추가 자격 없이 지원 가능"],
                블라인드=job in BLIND_JOBS,
            ))
        track_detours.sort(key=lambda d: -d["경쟁배수"])

    # (2) 전형 우회 — 사용자가 실제 해당하는 전형만
    가산 = student.get("가산", {})
    my_tracks = []
    if 가산.get("장애인"):
        my_tracks.append("장애")
    if 가산.get("취업지원대상자"):
        my_tracks.append("취업지원")
    track_switch = []
    if my_tracks:
        # 1순위 + 상위 추천 직무에 대해 일반↔해당전형 격차
        for r in recs:
            s = job_stats.get(r["직무"], {})
            gen = s.get("일반_가중경쟁률")
            for t in my_tracks:
                d_t = s.get("사회배려_세부", {}).get(t)
                if gen and d_t and d_t.get("경쟁률"):
                    track_switch.append(dict(
                        직무=r["직무"], 전형=t,
                        일반경쟁률=gen, 전형경쟁률=d_t["경쟁률"],
                        경쟁배수=round(gen / d_t["경쟁률"], 1),
                        표본n=d_t["n"], 참고용=d_t.get("참고용", False),
                    ))
        track_switch.sort(key=lambda d: -d["경쟁배수"])

    return dict(직렬우회=track_detours[:2], 전형우회=track_switch[:3])


if __name__ == "__main__":
    ds = bd.build_dataset()
    samples = [
        dict(이름="A(전기전공 2학년)", 학년=2, 전공계열="전기·전자",
             보유자격=[], 어학={"토익": 700}, 사회배려=False),
        dict(이름="B(상경 3학년)", 학년=3, 전공계열="상경·행정·법",
             보유자격=["컴퓨터활용능력 1급"], 어학={"토익": 850}, 사회배려=False),
        dict(이름="C(영문 2학년·사회배려)", 학년=2, 전공계열="인문·사회·기타",
             보유자격=[], 어학={"토익": 790}, 사회배려=True),
    ]
    for smp in samples:
        print(f"\n=== {smp['이름']} ===")
        for r in recommend(smp, ds=ds):
            tag = " [블라인드]" if r["블라인드"] else ""
            print(f"  [{r['적합도']}점] {r['직무']}{tag}  일반 {r['데이터']['일반경쟁률']}:1  "
                  f"근거 {r['구성']}")
            rm = r["로드맵"]
            print(f"        {rm['필기목표']} | 자격 {rm['취득권장자격']}")
            if rm["어학"]: print(f"        어학: {rm['어학']}")
            if rm["사회배려안내"]: print(f"        ★ {rm['사회배려안내']}")
